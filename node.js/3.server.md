# Creating a Simple Web Server with Node.js

You can use Node.js to create a simple web server using the built-in HTTP module.

## Example: Hello Node.js

The following example creates a web server that listens for HTTP requests on [http://127.0.0.1:8000/](http://127.0.0.1:8000/). When a request is received, the server responds with the string: **"Hello World"**.

### Steps to Try the Example

1. **Open Terminal**  
   On Windows, open the command prompt.

2. **Create a Project Folder**  
   ```bash
   cd path\to\your\projects
   mkdir test-node
   cd test-node
   ```

3. **Create the Server File**  
   Using your favorite text editor, create a file called `hello.js` and paste the following code:

   ```js
   // Load HTTP module
   const http = require("http");

   const hostname = "127.0.0.1";
   const port = 8000;

   // Create HTTP server
   const server = http.createServer((req, res) => {
     // Set the response HTTP header with HTTP status and Content type
     res.writeHead(200, { "Content-Type": "text/plain" });

     // Send the response body "Hello World"
     res.end("Hello World\n");
   });

   // Prints a log once the server starts listening
   server.listen(port, hostname, () => {
     console.log(`Server running at http://${hostname}:${port}/`);
   });
   ```

4. **Run the Server**  
   In your terminal, run:
   ```bash
   node hello.js
   ```

5. **View in Browser**  
   Open [http://localhost:8000](http://localhost:8000) in your web browser.  
   You should see the text **"Hello World"**.

---

This example demonstrates how easy it is to create a basic web server with Node.js using just a few 




Here are clean and simple **markdown notes** on ⚡ **Express.js** — great for learning or quick revision 👇

---




Here are clear and complete **markdown notes** on 🌐 **Creating Servers using Node.js** 👇

---

# 🌐 Creating Servers in Node.js

## 📘 Introduction

Node.js allows you to create **HTTP servers** using the built-in **`http`** module.
This helps you handle **client requests** and send **responses** — forming the basis of any backend application or API.

---

## 🧩 Importing the `http` Module

To create a server:

```js
const http = require('http');
```

---

## 🚀 Creating a Basic Server

### ✅ Example:

```js
const http = require('http');

// Create the server
const server = http.createServer((req, res) => {
  res.writeHead(200, { 'Content-Type': 'text/plain' }); // set header
  res.end('Hello, World! This is my Node.js server.');   // send response
});

// Start the server
server.listen(3000, () => {
  console.log('Server is running on http://localhost:3000');
});
```

### 🧠 Explanation:

* `http.createServer()` → creates an HTTP server.
* `(req, res)` → represents the **request** and **response** objects.
* `res.writeHead()` → sends response headers (status code + content type).
* `res.end()` → ends the response and sends data to the client.
* `server.listen(3000)` → server listens on **port 3000**.

---

## ⚙️ Handling Different Routes

You can use **conditional routing** to handle multiple pages or endpoints.

```js
const http = require('http');

const server = http.createServer((req, res) => {
  res.writeHead(200, { 'Content-Type': 'text/html' });

  if (req.url === '/') {
    res.end('<h1>Home Page</h1>');
  } 
  else if (req.url === '/about') {
    res.end('<h1>About Us</h1>');
  } 
  else {
    res.writeHead(404, { 'Content-Type': 'text/html' });
    res.end('<h1>404 Page Not Found</h1>');
  }
});

server.listen(4000, () => {
  console.log('Server running at http://localhost:4000');
});
```

---

## 📦 Serving JSON Data

You can send API responses using JSON.

```js
const http = require('http');

const server = http.createServer((req, res) => {
  if (req.url === '/api') {
    res.writeHead(200, { 'Content-Type': 'application/json' });
    const data = { name: 'Node.js', type: 'Server', version: 'v1.0' };
    res.end(JSON.stringify(data));
  } else {
    res.end('Go to /api to see JSON data');
  }
});

server.listen(5000, () => {
  console.log('Server running on http://localhost:5000');
});
```

---

## 🗂️ Serving HTML Files

You can use the **`fs`** module to serve HTML pages from files.

```js
const http = require('http');
const fs = require('fs');

const server = http.createServer((req, res) => {
  if (req.url === '/') {
    fs.readFile('index.html', (err, data) => {
      if (err) {
        res.writeHead(500);
        res.end('Error loading file');
      } else {
        res.writeHead(200, { 'Content-Type': 'text/html' });
        res.end(data);
      }
    });
  } else {
    res.writeHead(404, { 'Content-Type': 'text/plain' });
    res.end('Page Not Found');
  }
});

server.listen(3000, () => {
  console.log('Server running on http://localhost:3000');
});
```

---

## 🧠 Key Concepts

| Concept               | Description                                                            |
| --------------------- | ---------------------------------------------------------------------- |
| `req`                 | The **request object** — contains info like URL, headers, method, etc. |
| `res`                 | The **response object** — used to send data to the client.             |
| `res.writeHead()`     | Sends HTTP response headers (status & content type).                   |
| `res.end()`           | Ends the response (must be called).                                    |
| `server.listen(port)` | Starts the server on the specified port.                               |

---

## ⚡ Bonus: Creating a Server Using Express (Simplified)

Most modern Node.js apps use **Express.js** to simplify server creation.

```bash
npm install express
```

```js
const express = require('express');
const app = express();

app.get('/', (req, res) => res.send('Hello from Express!'));
app.get('/about', (req, res) => res.send('About Page'));

app.listen(3000, () => console.log('Server running on http://localhost:3000'));
```

---

## 🧩 Summary

| Operation               | Method / Concept               |
| ----------------------- | ------------------------------ |
| Create HTTP server      | `http.createServer()`          |
| Send text/HTML          | `res.write()` / `res.end()`    |
| Handle routes           | `req.url`                      |
| Send JSON data          | `res.end(JSON.stringify(obj))` |
| Serve files             | `fs.readFile()`                |
| Modern server framework | `express`                      |

---

Would you like me to include a **diagram showing the request–response flow** between the client, Node.js server, and browser?
